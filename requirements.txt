# 求职风向 Agent · Web Demo 依赖
jinja2>=3.1.0
openai>=1.0.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
curl_cffi>=0.15.0

# 企查查城市供给爬取：请用 Python≥3.11 的 .venv-crawl（browser-use 不支持 3.10）
# python3.11 -m venv .venv-crawl && .venv-crawl/bin/pip install playwright browser-use langchain-openai
# playwright>=1.40.0
# browser-use
# langchain-openai>=0.2.0

# 本地开发测试（生产可省略）
pytest>=8.0.0
